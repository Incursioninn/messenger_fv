<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/colorPrimary"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="?attr/actionBarSize">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/toolbar_chats_image"
        android:layout_width="@dimen/toolbar_chats_image_width"
        android:layout_height="@dimen/toolbar_chats_image_height"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@drawable/ic_menu_contacts"
        />

    <TextView
        android:id="@+id/toolbar_chats_fullname"
        style="@style/normal_text"
        tools:text="Moi drug"
        app:layout_constraintStart_toEndOf="@id/toolbar_chats_image"
        app:layout_constraintTop_toTopOf="@id/toolbar_chats_image"/>

    <TextView
        android:id="@+id/toolbar_chats_status"
        style="@style/small_text"
        tools:text="В сети"
        app:layout_constraintStart_toEndOf="@id/toolbar_chats_image"
        app:layout_constraintTop_toBottomOf="@id/toolbar_chats_fullname"/>

</androidx.constraintlayout.widget.ConstraintLayout>